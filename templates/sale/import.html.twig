{% extends 'base.html.twig' %}



{% block title %}{{"Les ventes" |trans}}{% endblock %}

{% block body %}

     <div class="container">

        <h1 class="titre-page">{{"Importation de ventes"|trans}}</h1>

        {% for message in app.flashes("error") %}
            <div class="alert alert-danger alert-dismissible m-3">
                {{ message }}
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>

            </div>
        {% endfor %}


        {{ form_start(form) }}

        {{ form_row(form.file, {
            label: 'Fichier (XLSX)'|trans
        }) }}

        <div class="d-flex justify-content-center m-5">

            {{ form_row(form.submit) }}
            <a href="{{ path("app_vente_index") }}" class="btn btn-danger btn-lg m-2 ">{{"Annuler"|trans}}</a>
        </div>
    
        
        {{ form_end(form) }}


        <h2> {{"Exemple de fichier"|trans}} :</h2>

        <p>{{"Le fichier doit contenir les colonnes suivantes"|trans}} : </p>

        <ul style="color:white !important ;">
            <li>{{"Catégorie"|trans}}</li>
            <li>{{"Libellé"|trans}}</li>
            <li>{{"Commentaire"|trans}}</li>
        </ul>

        <a href="{{ path('sale_model') }}" class="btn btn-outline-success m-2 ">{{"Télécharger le modèle"|trans}}</a>


    </div>


{% endblock %}